# Your Medusa backend. Remember to add this app's dev origin (default http://localhost:5173)
# to STORE_CORS in backend/.env.
VITE_MEDUSA_BACKEND_URL=http://localhost:9000

# Required. Grab this from Medusa admin (Settings > API Key Management) or, for local
# dev against this repo's seed data, curl http://localhost:9000/key-exchange
# (a browser can't fetch that route directly — it isn't covered by STORE_CORS).
VITE_MEDUSA_PUBLISHABLE_KEY=
